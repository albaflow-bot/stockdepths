# Learnings — 자동 추출 (Harness 2.0 H4)
본 파일은 빈데스크 chain 종료 시 자동 추출됩니다. 일반화 가능한 패턴 + 액션만 박제됨. 사용자 수동 편집 OK.

## 2026-06-22 11:58 UTC · round 1c5bf066-f033-4e74-99c0-20e0ef33867b
- 패턴: 멀티턴 플로우에서 이전 단계 메타데이터(선택/판정)를 다음 단계 프롬프트에 주입; 액션: 단계별 audit기록 + 메타 직렬화(JSON) + 다음호출 인자에 embed
- 패턴: 모든 자동화(AI평가/API판정/추천) 뒤에 사용자 override(다른선택/입력/스킵) 3분기 강제; 액션: fallback 컴포넌트로 선택지분기, 선택지부재 시 완결착시 신호
- 패턴: 외부API 빈응답(0건/null) 시 진행차단 ✗ graceful fallback 제공; 액션: 빈응답 분기 = skip/사용자입력/대체후보 옵션 미리계산
- 패턴: 제품이 주장하는 준칙을 본체 SPEC/사례에서 먼저 입증; 액션: 신규 준칙 문서화 후 즉시 자체 적용사례 추가(선제신뢰)
- 패턴: 의사결정 모달(필수선택) ≠ 백엔드 통신, 강제진행(닫기불가) 과 낙관적업데이트(에러복구) 분리; 액션: 모달=isClosable=false, 통신=optimisticUpdate+rollback

## 2026-06-24 09:24 UTC · round 28316018-a740-4324-bef7-fa9a5cc2e138
- 패턴: 빈 입력 폼("입력하세요")은 진입 거부감을 유발 — 진입 즉시 데이터가 흐르는 "골라 담기" 대시보드로 전환해야 완성; 액션: 신규/재구성 화면은 빈 상태에서도 시장·후보 데이터로 살아있게 설계하고 직접입력은 접힌 보조로 격하
- 패턴: 모든 종목/결과 표면이 한 줄 행동 신호(매수/매도/관망)로 환원되지 않고 정보 나열로 끝나면 미완성; 액션: 데이터 패널마다 한 줄 근거 동반 행동 배지를 공통 컴포넌트로 강제, 근거 없는 신호는 렌더 금지
- 패턴: 사후 검증(적중률·성적표) 가능성은 신호를 append-only immutable로 기록해야만 성립; 액션: 신호/추천 로그는 UPDATE/DELETE 없이 같은 배치 트랜잭션에 confidence NOT NULL로 박제
- 패턴: 추가 산출물(brief+방향성)을 별도 LLM 호출로 만들면 비용이 증식; 액션: 이미 같은 컨텍스트를 읽는 기존 단일 배치 호출에 합산해 추가 호출 0 유지
- 패턴: 외부 무료·합법 데이터 경로(KR 시세 등)는 미검증 단정이 위험한 open question; 액션: 경로 실증 결과를 태스크 산출에 남기고 미결정은 사용자 결정 큐에 노출(완결 착시 차단)
